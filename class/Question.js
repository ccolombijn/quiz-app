class Question {
  constructor( id, name, points, question, category ) {
    this._id = id
    this._name = name
    this._points = points
    this._question = question
    this._category = category
    
  }
}
