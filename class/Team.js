class Team {
  constructor( id, name, players, pointsTotal, color ) {
    this._id = id
    this._name = name
    this._players = players
    this._pointsTotal = pointsTotal
    this._color = color
  }
}
